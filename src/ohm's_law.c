#include <stdio.h>

int main() {
    // Declare variables
    int R = 10;
    float V = 3.3;
    float I = V / R; // Calculate current using Ohm’s Law: I = V/R

    // Print the result
    printf("The value of current, I is %.2f A\n", I);

    return 0;
}
